#ifndef RSA_RSA_HPP_
#define RSA_RSA_HPP_

#include <deque>

namespace rsa
{
	extern "C" auto main(int, char**) -> int;
	using cycles  = unsigned int;
	using mcycles = double;
}

#endif // RSA_RSA_HPP_
